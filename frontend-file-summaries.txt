{
  "frontend/src/index.css": "Global styles with OKLCH teal/amber color palette, Nunito/Poppins fonts, large base font sizes (18px), WCAG AA contrast, accessible focus indicators, and custom pulse animations for mic/TTS states.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Nunito/Poppins font families, larger font scale, teal primary and amber accent color tokens, success/warning colors, card shadows, and extra border-radius utilities.",
  "frontend/src/data/lessons.ts": "Lesson data with 5 categories — removes the `sign-language-asl` category entirely, keeping Alphabet, Common Phrases, Numbers, Vocabulary, and Sign Language (ISL).",
  "frontend/src/hooks/useTTS.ts": "Custom hook encapsulating browser SpeechSynthesis API — exposes speak(), stop(), isSpeaking state, available voices list, and browser support check.",
  "frontend/src/hooks/useSTT.ts": "Custom hook encapsulating browser SpeechRecognition API with fully self-contained TypeScript interfaces (no circular references), exposing startRecognition, stopRecognition, clearTranscript, isListening, interimTranscript, finalTranscript, error, and isSupported.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations: session key management, fetching all/single lesson progress, and mutations for startLesson, updateScore, and completeLesson.",
  "frontend/src/components/Navbar.tsx": "Updated Navbar replacing the `BookOpen` logo icon with `GraduationCap` from lucide-react; all navigation links and auth controls remain intact.",
  "frontend/src/components/LessonCard.tsx": "Reusable lesson card component showing category icon, title, description, progress bar, and score — navigates to lesson exercise on click.",
  "frontend/src/components/Footer.tsx": "App footer with copyright year, caffeine.ai attribution link with UTM params, and a heart icon.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard page with `BookOpen` removed from the stats bar — the \"Lessons started\" stat now uses plain text label only, with all other sections unchanged.",
  "frontend/src/pages/TTSTool.tsx": "Full-featured Text-to-Speech page with textarea input, voice selector, rate slider, Speak/Stop buttons, sample texts, speaking animation, and usage tips.",
  "frontend/src/pages/STTTool.tsx": "Speech-to-Text tool page with microphone toggle and live transcript; detects `not-allowed` microphone errors and shows a prominent amber-styled warning banner with step-by-step instructions to grant microphone access, while other errors show a generic destructive alert without exposing raw error strings.",
  "frontend/src/pages/LessonExercise.tsx": "Interactive lesson exercise page with `HandMetal` removed from imports and the sign language section header; replaced with the already-present `Camera` icon for the ASL Sign label.",
  "frontend/src/pages/Help.tsx": "Comprehensive help page with `BookOpen` and `HandMetal` icons removed from section headings; \"About SilentLearn\" and \"Lessons\" headings are now plain `<h2>` text, and the \"ISL Sign Language Recognizer\" heading is also plain text without an icon.",
  "frontend/src/App.tsx": "Root app component with TanStack Router registering `/login` and `/signup` routes pointing to the new Login and SignUp page components, alongside all existing routes.",
  "frontend/src/pages/SignCamera.tsx": "Sign Camera page with `HandMetal` removed from imports and all usages; the detected sign panel header is now plain text, and the empty-state icon is replaced with `Hand` from lucide-react.",
  "frontend/src/hooks/useHandGestureRecognition.ts": "Simplified hand gesture recognition hook that exclusively uses the ISL classifier (`recognizeISLGesture`) — removes the `GestureMode` type, `mode` parameter, and all ASL routing logic while preserving the full hook interface.",
  "frontend/src/utils/gestureMapping.ts": "ISL-only gesture mapping utilities — removes all ASL pattern definitions and the `mapLandmarksToGesture` function, keeping only ISL patterns, `recognizeISLGesture`, shared helpers (normalize, finger extension, pattern similarity), and `drawLandmarks`.",
  "frontend/src/pages/Login.tsx": "Login page with SilentLearn branding and \"Login with Internet Identity\" button; uses `isLoginError` (instead of `loginStatus === 'error'`) from `useInternetIdentity` to display error state, and redirects authenticated users to the dashboard.",
  "frontend/src/pages/SignUp.tsx": "Sign Up page with SilentLearn branding, Internet Identity explanation cards, and \"Create Account with Internet Identity\" button; uses `isLoginError` (instead of `loginStatus === 'error'`) from `useInternetIdentity` to display error state, and redirects authenticated users to the dashboard."
}